name: "custom-mapping-api"
description: "Toy API for custom route mapping testing - matches custom_mapping_remote"
port: 1234

routes:
  # Root endpoint for API metadata
  - path: "/"
    methods: ["GET"]
    response: "api_info"

  # Custom endpoint structure to test route mapping
  - path: "/user-permissions/<user_id>"
    methods: ["GET"]
    response: "user_permissions"

  - path: "/users/<user_id>"
    methods: ["GET"]
    response: "user_detail"

  - path: "/profiles/<user_id>"
    methods: ["GET"]
    response: "user_profile"

  # Additional routes for comprehensive testing
  - path: "/users"
    methods: ["GET"]
    response: "user_list"

  - path: "/users/<user_id>/settings"
    methods: ["GET"]
    response: "user_settings"

  - path: "/users/<user_id>/delete"
    methods: ["DELETE"]
    response: "user_detail"

  - path: "/health"
    methods: ["GET"]
    response: "health_check"