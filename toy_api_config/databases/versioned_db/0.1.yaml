name: versioned_db
description: Versioned test database for toy_api - Version 0.1

config:
  NB_USERS: 10

shared:
  user_id[[NB_USERS]]: UNIQUE[int]

tables:
  users[[NB_USERS]]:
    user_id: [[user_id]]
    name: NAME
    email: str
    active: bool
