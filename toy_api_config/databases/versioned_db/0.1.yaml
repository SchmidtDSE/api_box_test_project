name: versioned_db
description: Versioned test database for toy_api - Version 0.1 (custom table definitions)

config:
  NB_USERS: 10

shared:
  user_id[[NB_USERS]]: UNIQUE[int]

tables:
  # Version 0.1: Using custom table definitions (no object references)
  users[[NB_USERS]]:
    user_id: [[user_id]]
    name: NAME
    email: str
    active: bool
